import React from 'react';

const CampaignSchedule = () => {
    return (
        <div>
            <h2 className="text-2xl font-semibold mb-4 text-primary-text">Timeline & Schedule</h2>
            <p className="text-secondary-text">View and adjust the campaign timeline, milestones, and deadlines.</p>
        </div>
    );
};

export default CampaignSchedule;
